# **🚀 Project Status Report: GitPR CLI — v0.0.13 (2026-09-08)**

## **📌 Overview**

**GitPR** is an advanced CLI (Command Line Interface) tool for automating Git processes using Artificial Intelligence (Google Gemini / DeepSeek / Ollama). Its main goal is to act as a local intelligent assistant that performs Code Reviews, generates Pull Requests, semantic commit messages, audits technical debt, and injects best practices into the developer workflow (Shift Left).

**What's New in This Version (v0.0.13):**
- **SCM Multi-Forge (`gitpr --init` + `ScmProvider` layer):** A single abstraction over GitHub, GitLab, Bitbucket and Azure DevOps in `src/infrastructure/scm/` — registry with `resolve_scm_provider()` (default `github`, legacy token fallback intact), `parse_repo_ref()` for addressing repositories, providers that raise `ScmProviderError`, and an `--init` wizard that detects the forge from the remote, validates the token (`test_connection`, 3 attempts, re-prompt on 401) and persists **only on success** with Fernet encryption. `src/github_api.py` became a deprecated shim that delegates to the provider.
- **Suggested Reviewers in the PR flow:** Suggestion of reviewers from the forge itself when publishing PRs, with flag opt-out (`--no-suggest-reviewers`) and environment configuration (`GITPR_SUGGEST_REVIEWERS`, `GITPR_REVIEWER_SUGGESTION_TOP_N`, `GITPR_REVIEWER_SUGGESTION_EXCLUDED`).
- **`gitpr release` Subcommand (Changelog / Release Notes):** Generates the branch changelog between `--since` (default: last tag) and `HEAD`, classifies commits by Conventional Commits (`src/commit_classifier.py`), suggests a semantic bump (`src/version_bump.py`), assembles the translatable sections (`src/changelog_builder.py`), adds an AI executive summary and *prepends* it to `CHANGELOG.md`. With `--publish`/`--draft` it publishes the release to the forge (GitHub creates the tag; GitLab requires an existing tag); `--format markdown|json` for structured output. Skill template `gitpr.release.*.md` downloaded automatically on first use, in 5 languages.
- **Silent MCP Server + Bounded DNS:** Fix for tool output leaking in the stdio/CLI flow and time-bounded DNS resolution (bug from the previous window). `GITPR_AI_TIMEOUT` default reduced from 600s to **180s**.
- **Localized URLs and Prompts:** Repository URLs standardized in the templates and documentation; issue creation prompts now get the active language.
- **i18n expanded to 742 keys:** Changelog headings are now translatable (runtime helper, not opaque constants), 48 new keys translated across the 6 dictionaries, `__lang_version__` v0.0.23 and a bullet for the `release-notes` family in the README index (5 copies).
- **Multilingual Documentation Expanded:** 3 new complete families in 5 languages — `release-notes`, `scm-multiforge` and `suggested-reviewers` (+ architecture ADRs for SCM and release) — and 8 topics updated.
- **Version Jump:** `__version__` went from 0.0.37 to **1.0.0** (via 0.0.38 in this window); CHANGELOG.md records the header `[v0.1.0] - 2026-09-07` generated by the release feature itself during development.

- **Current version:** 1.0.0
- **Language dictionaries version:** v0.0.23
- **Hook scripts version:** v0.0.3
- **Distribution:** PyPI (`pip install gitpr-cli`) + GitHub Releases (standalone binary)
- **Website:** [gitpr.natanfiuza.dev.br](https://gitpr.natanfiuza.dev.br/)
- **Repository:** [https://github.com/gitpr-cli/gitpr.git](https://github.com/gitpr-cli/gitpr.git)
- **License:** LGPL-2.1
- **Supported languages:** en_us, pt_br, pt_pt, es_es, fr_fr (5 languages, 6 dictionaries)

---

## **🏗️ Base Architecture and Libraries**

* **Language:** Python >= 3.10
* **CLI Framework:** Click (for commands, flags, and terminal formatting).
* **UI/Terminal:** Textual — TUI (Text User Interface) for interactive chat, issue editing, help screen, metrics dashboard, PR Publisher and linter errors (`LinterApp`).
* **Cryptography:** `cryptography.fernet` for local protection of API keys, GitHub tokens and SCM tokens for the forges.
* **Configuration:** `python-dotenv`, `pyyaml` (for the static linter).
* **AI Providers:** Integration via the official Google GenAI SDK (`gemini-2.5-flash`), OpenAI SDK (`DeepSeek`), and OpenAI SDK (local `Ollama`).
* **Forge APIs:** `requests` (REST) — multi-forge abstraction layer in `src/infrastructure/scm/` (GitHub, GitLab, Bitbucket, Azure DevOps); legacy `src/github_api.py` module kept as a deprecated shim.
* **MCP:** [mcp](https://pypi.org/project/mcp/) >= 1.0.0 (official Anthropic SDK for Model Context Protocol) — 12 annotated tools, 17 resources, 7 prompts; handlers offloaded to threads via `anyio`.
* **Tests:** Pytest + `unittest.mock` (41 test files — 32 at the root + 9 in `tests/scm/` —, 791 collected scenarios) + MCP server e2e tests via real subprocess (JSON-RPC stdio).
* **Packaging:** PyInstaller (standalone binary) + setuptools/build (PyPI).
* **CI/CD:** GitHub Actions (`pr-review.yml`) + `action.yml` for execution in pipelines.

---

## **🧩 Implemented Modules and File Architecture**

### **1. Core and Git Operations (`src/core.py`)**

* **Structured Generation:** Communicates with the LLM requesting strictly JSON output.
* **Map-Reduce (Giant Diffs):** When the diff exceeds ~90k tokens, automatically splits it into batches by file (`split_diff_into_chunks`), processes each part (Map) and unifies the summaries (Reduce). Supports PRs, commits and Issues.
* **Local Tokenizer:** `tokenizer.json` for accurate token estimation before sending to the AI.
* **Token Estimation:** Lightweight `len() // 4` heuristic via `estimate_token_count()` with fallback to the local tokenizer.
* **Native Git Optimization:** `-U1`, `-w`, `-M`, `-B` flags on the `get_git_diff` and `get_git_full_diff` commands to reduce useless context.
* **Pre-Save (`--pre-save`):** Hidden debug flag that saves the full payload (system instruction + prompt) as JSON before each AI call.
* **Two-Layer Smart Excludes:** Smart pathspec filter with global (`~/.gitpr/conf/`) + local project (`./.gitpr/conf/`) layers. Runtime merging (union, deduplicated). Auto-seeding of the local file on first run.
* **Metrics with Time Tracking:** Injection of `log_command_metric()` into all flows with millisecond duration reporting (`duration_ms`) and lazy imports.
* **Centralized Output Resolution:** `resolve_output_path()` function that centralizes output directory logic — default in `.gitpr/reports/{type}/`.
* **SCM Wizard (`run_scm_init_wizard()`) 🆕:** `gitpr --init` — detects the forge from the origin remote, requests per-forge extras (Azure org/project, Bitbucket username), validates the token with `test_connection` (3 attempts, 401 re-prompt) and persists `GITPR_SCM_PROVIDER` + `GITPR_SCM_TOKEN_ENCRYPTED` (Fernet) **only on success**.
* **Release Skill Template (`ensure_release_skill_template()`) 🆕:** Downloads `templates/gitpr.release.*.md` on first use of `gitpr release` (CLI layer, language-aware, never overwrites; skipped on `--format json`).
* **Co-Authorship Trailer:** `COAUTHOR_TRAILER` + `append_coauthor_trailer()` — idempotent, preserves third-party trailers.
* **Hardened Subprocesses:** `stdin=subprocess.DEVNULL` + `encoding='utf-8'`/`errors='replace'` on every `subprocess.run`; connection check via socket `8.8.8.8:53` before network operations.

### **2. Global Plugin System (`src/plugins.py`)**

* **Plugin Architecture:** Extensibility system that loads plugins from the `~/.gitpr/plugins/` directory, applying to **all projects**.
* **Linter Plugins (`linter/`):** `.yml` files with additional regex rules merged with the local `.gitpr.linter.yml`.
* **MCP Prompt Plugins (`prompts/`):** `.md` files that extend the system context with specific instructions.
* **Factory Closures:** `get_linter_plugins` and `get_prompt_plugins` functions with closures to isolate state between sessions.
* **`--plugins` Command:** Lists all installed global plugins with their types and paths.
* **Multilingual Documentation:** `docs/plugins-system.md` in 5 languages (EN, PT-BR, PT-PT, ES, FR).

### **3. CLI Interface and Setup (`src/main.py` and `src/config.py`)**

* **Initial Setup:** Detects the first run, creates the `~/.gitpr/` folder, and interactively requests API keys, preferences and language.
* **Command Routing:** Manages all flags and the `release` subcommand (see module 22).
* **Default Behavior:** Running `gitpr` without flags opens the PR Publisher TUI.
* **Flags (35 Click options at the root):**
  * `--init` 🆕: Opens the SCM multi-forge configuration wizard (forge detection + token validation).
  * `--no-suggest-reviewers` 🆕: Turns off reviewer suggestion in the PR publishing flow.
  * `--no-publish`: Generates the PR description and saves it locally without opening the interactive editor.
  * `--no-edit`: Skips the TUI entirely — auto-commit, auto-push and publishes straight to GitHub.
  * `--base <branch>`: Overrides the Pull Request target branch.
  * `--plugins`: Lists installed global plugins.
  * `--linter-setup`: Opens the interactive external linter configuration assistant.
  * `--version`: Displays the current GitPR version (via `@click.version_option`).
* **Environment Variables (39 keys in `DEFAULT_CONFIG`):** SCM family 🆕 (`GITPR_SCM_PROVIDER`, `GITPR_SCM_TOKEN`, `GITPR_SCM_TOKEN_ENCRYPTED`, `GITPR_SCM_BASE_URL`, `GITPR_SCM_ORGANIZATION`, `GITPR_SCM_PROJECT`, `GITPR_SCM_USERNAME`), reviewers family 🆕 (`GITPR_SUGGEST_REVIEWERS`, `GITPR_REVIEWER_SUGGESTION_TOP_N`, `GITPR_REVIEWER_SUGGESTION_EXCLUDED`), plus `GITPR_AUTO_COMMIT`, `GITPR_SKIP_LINT`, `GITPR_AUTO_STAGE`, `GITPR_SKIP_UNSTAGED_CHECK`, `GITPR_SHOW_LOGS`, `GITPR_AUTO_MERGE`, `GITPR_SKIP_SMART_EXCLUDES`, `GITPR_SMART_EXCLUDES_GLOBAL`, `GITPR_SMART_EXCLUDES_LOCAL`, `GITPR_AI_TIMEOUT` (default 180s in this window), `OUTPUT_FILE_NAME_*`, `GITPR_COAUTHOR` (read-only) and others.
* **Contextual Help:** `-h --flag` displays feature-specific documentation with a direct (language-aware) link to GitHub. 🆕 Subcommands get their own `epilog=`: `gitpr release -h` ends with "Full documentation:" + `get_doc_url("release-notes.md")` (Click `\b` paragraph so the URL is not re-wrapped under any locale).
* **--lang:** Forces the interface language for the current run without persisting the change.
* **--provider:** Forces the AI provider (`gemini`, `deepseek`, `ollama`) for the current run.
* **--mcp:** Starts the MCP server on the stdio transport for editor integration — **12 annotated tools + 17 resources + 7 prompts**.
* **--install:** Guided 4-step assistant that downloads skill templates, installs Git Hooks, configures MCP in editors and validates API keys.
* **--metrics:** Local telemetry system with per-repository scope: `--export`, `--purge`, `--dashboard`.
* **--status:** Lists uncommitted files categorized (new/modified/deleted) — fast, no AI, no network.

### **4. PR Publisher TUI (`src/ui/pr_publish_app.py` and `src/ui/pr_publish_help.py`)**

* **Full Interactive Interface:** TUI built with Textual to review, edit and publish Pull Requests directly in the terminal.
* **6 Modal Screens:** `StageFilesScreen`, `CommitConfirmScreen`, `CommitProgressScreen`, `CommitMessageScreen`, `LinterErrorScreen`, `ErrorScreen`.
* **Suggested Reviewers 🆕:** The publishing flow queries the forge for suggested reviewers and offers them in the TUI; selection controlled by `--no-suggest-reviewers`, `GITPR_SUGGEST_REVIEWERS`, `GITPR_REVIEWER_SUGGESTION_TOP_N` and `GITPR_REVIEWER_SUGGESTION_EXCLUDED`.
* **Bindings:** F1 (Help), F2 (Save local .md), F3 (Publish via GitHub API), Esc (Quit).
* **Auto-Commit Flow:** Linter → AI message → confirm → commits → push → publishes PR.
* **Unstaged Files Check:** On start, checks `git status --porcelain` and offers a modal to select, skip or cancel.
* **Existing PR Handling:** Detects open PRs for the current branch via API and offers to push or create a new one.
* **Auto-Upstream:** Detects a `git push` failure caused by a missing upstream and automatically tries `--set-upstream origin <branch>`.
* **Merge Flow:** After creating/updating the PR, offers a merge option. Controlled by `GITPR_AUTO_MERGE`.

### **5. GitHub API Module (`src/github_api.py`)**

* **Deprecated Shim 🆕:** `create_pull_request()`, `update_pull_request()`, `merge_pull_request()` and the other functions now delegate to `src/infrastructure/scm/github_provider.py`; the module emits a `DeprecationWarning` and keeps the legacy `(ok, data, status)` tuples — no new code may import it.

### **6. Static Analysis Engine / Linter (`src/linter_engine.py`)**

* **Offline Linter:** Statically analyzes the added lines (`+`) in the git diff without spending AI quotas.
* **YAML Rules:** Reads the local `.gitpr.linter.yml` file (created via `--skill`).
* **Linter Plugins:** Additional rules loaded from `~/.gitpr/plugins/linter/*.yml`.
* **External Linters Bridge:** Runs ESLint/PHPCS/Stylelint on the changed diff lines, Checkstyle XML parser and per-line cross-reference.
* **Consolidated Report:** `generate_linter_report_content()` consolidates regex + external errors into `.gitpr/reports/linter/` — generated only when there are violations.

### **7. Security and Authentication (`src/security.py`, `src/config.py`, `src/tui_issue.py`)**

* **Cryptography:** Generates a master key `secret.key` in the `~/.gitpr/` folder.
* **Token Protection:** `encrypt_data` and `decrypt_data` to protect AI API keys, GitHub PATs and SCM tokens for the forges (`GITPR_SCM_TOKEN_ENCRYPTED`).
* **Multi-Forge Validation 🆕:** `validate_or_request_scm_token(provider, repo_display)` — validates the token against the configured forge with 401 → re-authentication loop preserving the draft; the legacy GitHub token (`GITHUB_TOKEN_ENCRYPTED`) remains functional until `--init` runs.

### **8. Auto-Updater (`src/updater.py`)**

* **Hot-Swap:** Checks the GitHub Releases API for the latest version, downloads the compiled binary and replaces it without breaking the running execution (with rollback).
* **Daily Cache:** Avoids repeated checks on the same day.
* **Centralized Versioning:** `__version__` (**1.0.0**), `__lang_version__` (**v0.0.23**), `__scripts_version__` (**v0.0.3**), `SMART_EXCLUDES_VERSION`, `THINKING_WORDS_VERSION`, `LINTER_PRESETS_VERSION`.

### **9. Interactive Chat Interface (`src/ui/chat_app.py`)**

* **Full TUI:** Built with Textual — message history, multi-line input, status bar with visible bindings.
* **Per-Branch Memory (`src/chat_memory.py`):** Conversation history persisted per branch, allowing continuity between sessions.
* **Slash Commands:** `/explain`, `/tests`, `/optimize`, `/clear` — shortcuts for pair programming.
* **Auto-Patching (F5), Diff Refresh (F2), Session Export (F6).**

### **10. Internationalization — i18n (`src/i18n.py`)**

* **Laravel-Inspired System:** `__()` function with support for named placeholders (`{count}`, `{file}`, etc.).
* **Automatic Detection:** Detects the OS language on first run and saves it in `GITPR_LANG`.
* **5 Languages, 6 Dictionaries:** en_us (default/fallback), pt_br, pt_pt, es/es_es, fr/fr_fr.
* **Versioned Files:** `__lang_version__` (**v0.0.23**) controls updating of the language packs (`langs/*.json`) — bump chain v0.0.20 → v0.0.23 in this window.
* **Coverage:** **742 keys** of translation in each of the 6 files — **full key-set parity** (AST audit of 742 keys in code: 0 untranslated, 0 orphaned).
* **Translatable Changelog Headings 🆕:** The 8 changelog section headings (Features, Bug Fixes, Breaking Changes etc.) are no longer opaque constants and are now `__()` literals resolved at runtime via the `_category_heading()` helper — they follow `--lang`/`set_lang` and are visible to the AST extractor.
* **Genuine Translations 🆕:** +195 keys since the previous report (48 of them from the 2026-08-09 next-steps task, in real translation across the 6 dictionaries, CRLF preserved).
* **Cache with Language Indexing:** Cached AI responses include the current language in the MD5 keying.

### **11. Animated Spinner (`src/spinner.py`)**

* **Braille + Thinking Words:** Background thread during AI calls displaying braille characters with "thinking" words.
* **263 entries per language:** Synchronized across the 5 languages.

### **12. AI Providers (`src/ai_providers.py`)**

* **3 Supported Providers:** Google Gemini (`gemini-2.5-flash`), DeepSeek (`deepseek-chat`), Ollama (local).
* **JSON Mode & Deterministic Parameters:** Structured outputs with `temperature=0.0` and `top_p=0.1`; automatic fallback between configured providers.

### **13. Smart Cache (`src/cache.py`)**

* **MD5 + Metadata:** Keying by MD5 hash of the diff and prompt, with language indexing.
* **Telemetry and Duration:** Persistence of the `duration_ms` and `meta_raw` fields in cache files.
* **Reading for the Dashboard:** `scan_cache_files_for_dashboard()` reads all cache files recursively.

### **14. Issues Engine and TUI (`src/issue_engine.py`, `src/tui_issue.py`, `src/ui/issue_app.py`)**

* **3 Context Engines:** Current diff, Branch history (`-ht`), and Blame Archaeology (`-b`).
* **Multi-Forge Publishing 🆕:** F3 creates the issue on the **configured** forge (`GITPR_SCM_PROVIDER`) via `provider.create_issue` — not just GitHub; Azure DevOps raises `ScmNotSupportedError` (Work Items depend on the process template).
* **Map-Reduce for Issues:** When the context exceeds ~90k tokens, automatically splits into chunks and unifies the results.
* **401 Handling:** Re-authentication signalling without closing the application.

### **15. Code Archaeologist (`src/blame_engine.py`)**

* **Git Blame + AI:** Tracks the historical evolution and authorship of code snippets with commit classification (`ORIGIN` vs `REFACTORING`).
* **Blame Metrics:** Events recorded via `log_blame_metric()` with depth tracking and number of commits analyzed.

### **16. MCP Server and Direct CLI Invocation (`src/mcp_server.py`)**

* **12 Annotated MCP Tools:** Tools for `get_git_context`, `analyze_diff`, `list_unstaged_files`, `analyze_unstaged_diff`, `get_full_diff`, `generate_commit_message`, `review_code`, `full_review`, `generate_pr_description`, `run_linter`, `analyze_blame`, `generate_issue`.
* **17 Resources + 7 Templated Prompts:** `skill://list` + `skill://{pr,commit,review,filereview,issue,blame,release}` + `linter://config` + `prompt://list` + 7 prompts — 🆕 release skill resources (and the corresponding family in the templates).
* **Direct CLI Invocation:** The `gitpr-mcp --tool <name> [--tool-args '<json>']` command invokes any MCP tool directly without starting the stdio JSON-RPC server.
* **Real Stdout Isolation:** `_write_real_stdout()` writes directly to the original `sys.__stdout__`, guaranteeing pure JSON on stdout.
* **Guaranteed Silence 🆕:** Tool output silenced in the server/CLI flow (print leaks that corrupted the JSON-RPC stream were eliminated — fix `681a7fa`, PR #146).
* **Time-Bounded DNS 🆕:** DNS resolution of network operations is bounded in time — no blocking call gets stuck in resolution (alongside the hard OTA download timeout).
* **Event Loop Offload:** `_offload` decorator (`anyio.to_thread.run_sync`) applied to all 12 tools — synchronous handlers do not freeze the stdio server.
* **E2E Tests:** `tests/test_mcp_server_e2e.py` boots the real server as a subprocess and speaks JSON-RPC stdio.

### **17. Metrics Dashboard TUI (`src/ui/metrics_app.py`)**

* **Per-Repository Scope (Repo-Scope):** Label `📁 Repository: owner/repo` and strict per-project filtering.
* **Asynchronous Scan with Overlay:** Background worker thread with a `ProgressBar` widget.
* **Data Consolidation:** `load_cache_token_summary()` adds cache tokens to the totalizer.
* **Local Export:** Saving CSV/JSON to `./.gitpr/metrics/export/`.

### **18. Metrics and Telemetry System (`src/metrics.py`)**

* **Per-Repository Scope:** All events indexed by `repo_name`.
* **Hook, Linter and Blame Events:** `log_hook_event()`, `log_linter_metric()`, `log_blame_metric()`.
* **Export and Cleanup:** `--metrics --export` (CSV/JSON) and `--metrics --purge` with interactive confirmation.

### **19. Git Hooks Synchronization**

* **Independent Versioning:** `__scripts_version__` (v0.0.3) controls the version of the hook scripts; automatic detection and update.
* **Language-Aware:** Downloads hook templates matching the configured language.
* **Merge-Source Skip:** The `prepare-commit-msg` template skips the `message|merge|squash|commit` sources — commits generated by git preserve the original message.

### **20. External Linters Bridge and Interactive Assistant (`src/linter_wizard.py`, `src/ui/linter_app.py`)**

* **`--linter-setup` Assistant:** Interactive wizard with numbered presets (PHP_CodeSniffer, ESLint, Stylelint) and injection of the `external_linters` block into `.gitpr.linter.yml`.
* **Remote Presets:** `templates/gitpr.linter-presets.json` served from GitHub with the resolution chain local → download → stale → embedded fallback.
* **Linter Error TUI:** `src/ui/linter_app.py` (Textual) displays critical errors and warnings; in hook/quiet mode it prints and calls `sys.exit(1)`.
* **Markdown Report:** Consolidated into `.gitpr/reports/linter/` — only when there are violations.

### **21. SCM Multi-Forge (`src/infrastructure/scm/`) 🆕**

* **Single Abstraction (`ScmProvider` ABC):** `base.py` defines the contract (dataclasses `RepoRef`, `PullRequestDraft` etc. and `ScmProviderError(provider, http_status, message)` — `http_status` 0 = network failure); one concrete provider per forge in `github_provider.py`, `gitlab_provider.py`, `bitbucket_provider.py`, `azure_devops_provider.py`.
* **Registry and Factory:** `resolve_scm_provider()` selects by `GITPR_SCM_PROVIDER` (default `github` — zero migration, legacy GitHub token fallback intact); `detect_provider_from_remote()` identifies the forge from the origin URL.
* **Repository Addressing:** `parse_repo_ref(remote_url) -> RepoRef(raw, workspace, name, provider)` — workspace = GitHub owner / GitLab namespace (subgroups) / Bitbucket workspace / Azure `{org}/{project}` display.
* **Fail-Fast per Forge:** Azure DevOps requires `GITPR_SCM_ORGANIZATION`/`GITPR_SCM_PROJECT`; Bitbucket requires `GITPR_SCM_USERNAME` (App Password = Basic auth user+token); `create_issue` on Azure raises `ScmNotSupportedError`.
* **Release Publishing 🆕:** `provider.create_release()` used by `gitpr release --publish` (GitHub creates the tag on the default branch; GitLab requires the tag to exist) — `tests/scm/test_release_publish.py`.
* **Artifacts:** Glossary + ADR-001 in `docs/plans/`; `docs/scm-multiforge.*.md` family in 5 languages; tests: 9 files, 265 scenarios.

### **22. `gitpr release` Subcommand — Changelog / Release Notes 🆕**

* **Flow:** `git log` between `--since` (default: last reachable tag, or the first commit) and `HEAD` → Conventional Commits classification → suggested semantic bump (`--version <x.y.z>` overrides) → changelog assembly → optional AI executive summary → *prepend* to `CHANGELOG.md`. Local generation is the default — nothing is published or touched without request.
* **Classifier (`src/commit_classifier.py`):** Classifies commits by Conventional Commits type (feat/fix/refactor/docs/chore/etc.) with a tolerant parser — the basis for section grouping.
* **Builder with Translatable Sections (`src/changelog_builder.py`):** Sections "✨ Features", "🐛 Bug Fixes", "⚠️ Breaking Changes", "Summary", "Contributors" etc. rendered via `__()` at runtime (they follow `--lang`); AST audit arithmetic closed (742 = 742).
* **Semantic Bump (`src/version_bump.py`):** Suggests the next version from the classified types (major for breaking, minor for feat, patch for fix) and validates `x.y.z` targets.
* **Publishing:** `--publish` creates the release on the configured forge (with explicit confirmation); `--draft` creates it as a draft (GitHub; GitLab has no draft concept); `--format markdown|json` for structured output; `--force` to rewrite.
* **Skill Template:** On first use it downloads `templates/gitpr.release.*.md` (5 languages: en/pt_br/pt_pt/es_es/fr_fr) via `ensure_release_skill_template()` — never overwrites; locally editable as the system instruction for the executive summary.
* **Contextual Help:** `gitpr release -h` ends with "Full documentation:" + a language-aware link to `docs/release-notes.md` (epilog `\b`, no URL rewrap).
* **Tests:** 94 new scenarios — `test_release_cli.py` (4), `test_release_engine.py` (27), `test_changelog_builder.py` (15), `test_commit_classifier.py` (23), `test_version_bump.py` (17) + `tests/scm/test_release_publish.py` (8).
* **Artifacts:** `docs/release-notes.*.md` family (5 languages), spec in `docs/plans/`, ADR-002 (subcommand) and ADR-003 (flat modules), release notes glossary.

---

## **📊 Tests and Quality**

| Test File | Scenarios | Focus |
|------------------|----------|------|
| `tests/test_blame_engine_ranges.py` | 7 🆕 | Blame by line range in a file |
| `tests/test_blame_metrics.py` | 7 | Blame metrics: depth, commits, duration |
| `tests/test_changelog_builder.py` | 15 🆕 | Changelog builder: sections, translatable headings, contributors |
| `tests/test_chat_backend.py` | 19 | Chat memory, persistence, slash commands |
| `tests/test_commit_classifier.py` | 23 🆕 | Conventional Commits classification (types, tolerant parser) |
| `tests/test_config_suggest_reviewers.py` | 8 🆕 | Suggested reviewers configuration (keys and defaults) |
| `tests/test_core.py` | 39 | Main flows, git diff, PR generation, timing, staging, co-authorship |
| `tests/test_diff_parser.py` | 15 🆕 | Diff parser by lines/hunks |
| `tests/test_external_linters.py` | 33 | Checkstyle bridge: XML parser, subprocess, diff cross-reference, report |
| `tests/test_i18n.py` | 20 | Parity between languages (742×6), missing/orphaned keys, identity |
| `tests/test_install_wizard.py` | 3 | Interactive installation assistant |
| `tests/test_issue_engine.py` | 4 | Structured issue draft |
| `tests/test_linter_metrics.py` | 4 | Linter metrics: errors, warnings, duration |
| `tests/test_main_suggest_reviewers.py` | 6 🆕 | `--no-suggest-reviewers` flag in the CLI and contextual help |
| `tests/test_mcp_prompts.py` | 11 | MCP prompt templates and language fallback |
| `tests/test_mcp_server.py` | 85 | MCP tools, resources, annotations, patching, direct CLI, offload |
| `tests/test_mcp_server_e2e.py` | 6 | Real MCP server via subprocess + JSON-RPC stdio |
| `tests/test_metrics.py` | 34 | Collection, local export, repo scope, cache token summary |
| `tests/test_net_timeouts.py` | 12 | Network/AI timeouts — **2 outdated assertions (600s)** |
| `tests/test_plugins.py` | 17 | Plugin discovery, linter rule merge, MCP prompts |
| `tests/test_pr_publish_app.py` | 42 | PR Publisher TUI: screens, flows, suggested reviewers |
| `tests/test_pr_publish_linter_modal.py` | 4 | Linter error modal: abort, no-verify |
| `tests/test_pre_save.py` | 3 | --pre-save flag and JSON payload |
| `tests/test_release_cli.py` | 4 🆕 | Release CLI: options, help with documented epilog |
| `tests/test_release_engine.py` | 27 🆕 | Release engine: commit range, CHANGELOG, publishing |
| `tests/test_reviewer_suggestion.py` | 15 🆕 | Reviewer suggestion logic (ranking, exclusion, top-N) |
| `tests/test_skill_command.py` | 10 | Skill template download and validation |
| `tests/test_skill_context.py` | 6 🆕 | `get_skill_context()` with `quiet=True` (fallbacks, release) |
| `tests/test_smart_excludes.py` | 13 | Smart pathspec filter |
| `tests/test_suggest_reviewers.py` | 14 🆕 | Suggested reviewers in the PR flow (integration) |
| `tests/test_thinking_words.py` | 3 | Loading and parsing with the `;` separator |
| `tests/test_version_bump.py` | 17 🆕 | Semantic bump: major/minor/patch, targets and validation |
| `tests/scm/test_contract.py` | 38 🆕 | `ScmProvider` contract: signatures, dataclasses, errors |
| `tests/scm/test_github_provider.py` | 57 🆕 | GitHub provider: REST, headers, PRs, issues, releases |
| `tests/scm/test_gitlab_provider.py` | 41 🆕 | GitLab provider: API v4, namespace, releases |
| `tests/scm/test_bitbucket_provider.py` | 39 🆕 | Bitbucket provider: Basic auth, workspace |
| `tests/scm/test_azure_devops_provider.py` | 43 🆕 | Azure DevOps provider: org/project, PRs, `ScmNotSupportedError` |
| `tests/scm/test_factory.py` | 11 🆕 | `resolve_scm_provider()` + `detect_provider_from_remote()` |
| `tests/scm/test_github_api_shim.py` | 18 🆕 | Deprecated `github_api` shim → delegates to the provider (replaces `test_github_api.py`) |
| `tests/scm/test_init_wizard.py` | 10 🆕 | `--init` wizard: forge detection, validation, persistence |
| `tests/scm/test_release_publish.py` | 8 🆕 | Release publishing per forge (GitHub/GitLab) |
| `tests/sync_i18n.py` | — | i18n coverage verification script (scaffold; never executed) |

**Total:** 791 scenarios collected across 41 test files (32 at the root + 9 in `tests/scm/`; +527 since the previous report — includes the 265 SCM scenarios, which did not previously exist as a suite). Full run on this machine with `GITPR_LANG=en_us`: **787 passed / 2 failed / 2 skipped / 15 subtests** in ~63s.

**Quality notes for this version:**
- **2 real failures (outdated tests):** `test_net_timeouts.py` still asserts the 600s default for `GITPR_AI_TIMEOUT`, but the code switched to **180s** in this window (fix `681a7fa`) — see Next Steps.
- **4 environmental locale failures (pt-BR machine):** `test_chat_backend::test_api_exception`, `test_main_suggest_reviewers::test_flag_appears_in_contextual_help` and `test_suggest_reviewers` ×2 render pt_br text from the OTA copy of `~/.gitpr/langs/` — they pass fully with `GITPR_LANG=en_us`. The fully green suite from the previous report does not repeat in this version (2 test regressions + locale sensitivity).
- `test_github_api.py` was **removed** (legacy code became a shim) and succeeded by `tests/scm/test_github_api_shim.py` (18 scenarios).

---

## **🌐 Internationalization and Documentation**

* **i18n Coverage:** **742 keys** of translation in each of the 6 dictionaries (+195 since the previous report) with **full key-set parity** — AST audit of 742 keys used in code: 0 untranslated, 0 orphaned. The last 48 keys (next steps of the release-notes family: changelog headings, help strings, AI prompts with a real `\n` preserved) were translated with byte-exact CRLF preserved.
* **New Topics 🆕 (all 3 in 5 languages):**
  - `docs/release-notes.md` — `gitpr release` subcommand family (flow, versions, publishing, AI summary, skill template)
  - `docs/scm-multiforge.md` — `ScmProvider` layer and `--init` wizard (4 forges, tokens, per-forge limitations)
  - `docs/suggested-reviewers.md` — suggested reviewers in the PR flow (configuration and flags)
* **Topics updated in this window (all re-synced across the 5 languages):** `docs/auto-update.md`, `docs/github-ci-linter.md`, `docs/linter-regras-customizadas.md`, `docs/map-reduce-diff.md`, `docs/mcp-integration.md`, `docs/providers-ia.md`, `docs/skill-template.md`, `docs/smart-excludes.md` + `docs/ARCHITECTURE.md` (EN; record of the new families).
* **Documentation in 5 languages:** **37 canonical topics** in `docs/` — **32 with full coverage in the 5 languages** (+3 since the previous report) and 5 partial/PT-only topics (`como_reverter_commit_git_localmente`, `github-issue-prompt-com-gh`, `otimizacao-de-tokens`, `testar_sem_usar_pypi`, `version-markers` — the latter now counted explicitly).
* **Local Claude Code Skills:** `.claude/skills/` with `status-report`, `implement-fixes`, `caveman-commit`, `new-feature` and `reports-to-memory` (no new ones in this window).
* **Memory Index:** `.claude/memory/MEMORY.md` with 40 patterns (25 project, 9 feedback, 3 reference + 3 standalone).
* **Task Reports:** `docs/claude-code/reports/develop_natan/` (**84** in total; **+15** in the window — SCM multi-forge, suggested reviewers, docs for the 3 families, release notes/spec, release template skill, i18n next steps etc.) and `docs/gemini/reports/` (5 files today; +2 in the window: `2026-08-28_add_mcp_tools_to_gemini_md.md` and `2026-09-03_update_repo_urls.md` — the previous report's count (8) included files removed during the 2026-08-26 directory restore).
* **Status Reports:** `docs/reports/` (12 reports; this is the 13th).
* **Development Plans:** 80 files in `docs/plans/` (+21 in the window — specs and ADRs for SCM multi-forge, release notes, suggested reviewers, surveys and the release skill).

---

## **🔄 Distribution Pipeline**

1. **PyPI:** `python -m build` → `twine upload dist/*` → `pip install gitpr-cli`
2. **GitHub Releases:** PyInstaller → standalone `.exe` → automated upload
3. **GitHub Actions:** Workflow `pr-review.yml` + `action.yml`
4. **MCP Server:** `gitpr-mcp` entry point via `pyproject.toml`
5. **OTA Templates and Languages:** `templates/` and `langs/*.json` served from GitHub (main) — the `v0.0.23` bump renews the local copies in `~/.gitpr/langs/` once published

---

## **📈 Evolution Since the Previous Report (v0.0.12)**

| Area | v0.0.12 (previous) | v0.0.13 (current) |
|------|-------------------|----------------|
| **GitPR Version** | 0.0.37 | **1.0.0** (via 0.0.38 in the window; CHANGELOG.md with header `[v0.1.0]`) |
| **Language Version** | v0.0.20 | **v0.0.23** |
| **Hook Scripts Version** | v0.0.3 | **v0.0.3** |
| **AI Providers** | Gemini + DeepSeek + Ollama | Gemini + DeepSeek + Ollama |
| **Languages** | 5 languages, 6 dictionaries | 5 languages, 6 dictionaries |
| **Interface** | CLI + Issues TUI + Chat TUI + MCP Server + Dashboard + PR Publisher TUI + LinterApp + `--linter-setup` | **+ SCM wizard `--init` + `gitpr release` subcommand (CLI) + suggested reviewers in the PR Publisher** |
| **MCP Tools** | 12 tools (offload) | **12 tools (offload; silenced output + bounded DNS) — 17 resources (was 15)** |
| **CLI Flags** | 27 flags | **35 options at the root (+ `--init`, `--no-suggest-reviewers`; full Click count) + `release` subcommand with 6 options** |
| **Environment Variables** | 23 vars | **39 keys in `DEFAULT_CONFIG` (+ 7 SCM + 3 reviewers)** |
| **Linter** | Regex + Checkstyle bridge (wizard/TUI/report) | Unchanged |
| **Commit Messages** | With `Co-Authored-By` trailer (opt-out) | Unchanged |
| **i18n (keys per file)** | 547 × 6 (full parity) | **742 × 6 (full parity) — translatable changelog headings** |
| **Documentation** | 33 canonical topics (29 complete + 4 partial) | **37 canonical topics (32 complete + 5 partial) — 3 new families ×5, 8 updated** |
| **Test Suite** | 264 scenarios (17 files) | **791 collected scenarios (41 files: 32 + 9 SCM) — en_us: 787 passed / 2 failed (outdated) / 2 skipped** |
| **Commits since the report** | 17 commits | **10 commits** |
| **Merged PRs** | 8 PRs (#119–#135) + 2 PR_DESCs without reference | **5 PRs (#146, #151, #153, #155, #159)** |
| **Memory Index** | 32 patterns | **40 patterns (25 project / 9 feedback / 3 reference)** |
| **Task Reports** | 65 claude-code, 8 gemini | **84 claude-code (+15 in the window) and 5 gemini (+2; the previous count included files from the pre-restore of 2026-08-26)** |
| **Development Plans** | 59 | **80 (+21 in the window — specs, ADRs and surveys)** |

---

## **🚧 Next Steps**

* **Anthropic Claude Provider:** Direct support for the Claude API (`claude-sonnet-5`).
* **ASCII/Textual Charts in the Dashboard:** Add time histograms and token trend charts to the metrics TUI.
* **Release Pipeline in GitHub Actions:** Full automation of the PyInstaller build and asset upload to GitHub Releases (changelog generation is now local via `gitpr release` — CI/CD automation is still missing).
* **`.gitpr/conf/` Local Seed:** `--init` became the SCM wizard in this window; seeding local configuration templates (smart-excludes, linter) is still pending as its own subcommand or a wizard step.
* **More Providers:** Direct OpenAI, additional local providers.
* **i18n Extractor in `sync_i18n.py`:** The regex truncates literals with implicit concatenation (`__("a " "b")`) — migrate to AST (the guard in `test_i18n.py` already uses AST and does not depend on the script).
* **Fix the Outdated Timeout Tests 🆕:** `tests/test_net_timeouts.py` (lines ~99/117/137/149) asserts a 600s default, but the code has used 180s since `681a7fa`; also align the stale docstring in `config.py` (still mentions "default 600").
* **Reconcile the Project Version 🆕:** `CLAUDE.md` still says "Current version: 0.0.37"; `__version__` is at 1.0.0; CHANGELOG.md records `[v0.1.0] - 2026-09-07` (generated by the feature itself). Define a single convention and update CLAUDE.md.
* **README Index Debt 🆕:** The bullets for the `suggested-reviewers` and `scm-multiforge` families are not yet in the index (decision: only `release-notes` this round).
* **Locale Robustness in the Tests 🆕:** 4 tests are sensitive to the machine's pt_br locale (OTA copies of `~/.gitpr/langs/`) — pin `GITPR_LANG=en_us` in the setup or mock `TRANSLATIONS` so the suite is fully green on any machine/CI.

### ✅ Completed in This Window (2026-08-28 → 2026-09-08)

* ~~**MCP tools silence + bounded DNS**~~ — fix `681a7fa` (PR #146); `GITPR_AI_TIMEOUT` default 600s → 180s.
* ~~**Standardized repository URLs + localized issue prompts**~~ — `fa4bac1` (PR #151).
* ~~**Complete SCM multi-forge**~~ — providers, factory, `--init` wizard, deprecated shim, docs ×5 and the `tests/scm/` suite (PR #153).
* ~~**Suggested reviewers in the PR flow**~~ — flag, config, tests and docs ×5 (PR #155).
* ~~**`gitpr release` subcommand**~~ — modules, tests, skill templates ×5, docs ×5, ADRs and spec (PR #159, commit `b0e5d92`).
* ~~**Next steps for the release-notes family**~~ — contextual help `gitpr release -h` → docs; 48 i18n keys translated (742 × 6); translatable changelog headings; family bullet in the README index ×5 — see [task report](../claude-code/reports/develop_natan/2026-09-08_release_notes_next_steps.md).

---

**Report generated on:** 2026-09-08  
**Branch:** `develop_natan`  
**Author:** Natan Fiuza ([contato@natanfiuza.dev.br](mailto:contato@natanfiuza.dev.br))
